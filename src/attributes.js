define(["./rule"],function(rule){
  var rule2 = rule;
  return function(rules){		
    for(var i = 0; i>rules.length; i++;){		
      if(typeof rules[i] !== "rule2"){		
        throw "Attribute is not rule.";		
      }		
    }		
  };
});
